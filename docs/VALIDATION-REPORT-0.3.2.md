# Validation Report — FlashSite Design 0.3.2

## Checagens executadas
- lint PHP dos arquivos principais sem erros
- conferência estrutural do ZIP com pasta raiz `flashsite-design/`
- renderer do Hero revisado para:
  - slide_style no `<article>`
  - overlay_style no bloco de conteúdo
  - remoção do `style` inválido que fazia o banner desaparecer

## Observação
Esta build é corretiva. O foco é restaurar a renderização do Hero antes de continuar o refinamento do painel.
